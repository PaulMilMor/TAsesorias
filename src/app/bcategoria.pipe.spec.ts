import { BcategoriaPipe } from './bcategoria.pipe';

describe('BcategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new BcategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
